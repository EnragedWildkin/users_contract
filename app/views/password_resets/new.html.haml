- if target.errors.any?
  .alert.alert-danger
    %a.close{"aria-label" => "close", "data-dismiss" => "alert", href: "#"} &times;
    %strong
      #{pluralize(target.errors.count, "error")} prohibited this #{target.class.name.downcase}
      from being saved:
    %ul
      - target.errors.full_messages.each do |msg|
        %p= msg
